---
import { getCollection } from "astro:content";
import Card from "../../components/Card.astro";
const allCookies = await getCollection("cookies");
---

<section id="Recipes">
  <div class="recipes-container">
    <div class="category">
      <div class="title-container">
        <div class="title-emoji-wrapper">🍪</div>
        <h2 class="title">Kurabiye</h2>
      </div>

      <div class="grid">
        {
          allCookies.map((cookie, index) => (
            <Card
              title={cookie.data.title}
              description={cookie.data.description}
              tags={cookie.data.tags}
              color={cookie.data.color}
              image={cookie.data.image}
              link={`/cookies/${cookie.id}`}
              delay={index * 0.1}
            />
          ))
        }
      </div>
    </div>
  </div>
</section>

<style>
  section {
    padding: 7rem 2rem;
    background: rgba(255, 255, 255, 0.02);
  }

  .recipes-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .title {
    font-family: var(--font-heading);
    font-size: clamp(2rem, 4vw, 2.75rem);
    color: #fff;
    margin-bottom: 1.5rem;
  }

  .title-container {
    display: flex;
  }

  .title-emoji-wrapper {
    padding-right: 1rem;
    font-size: 2rem;
    align-self: center;
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(285px, 1fr));
    justify-content: center;
    gap: 1.5rem;

    animation: fadeInUp 0.8s ease forwards;
    animation-timeline: view();
    animation-range: entry 10% cover 30%;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
